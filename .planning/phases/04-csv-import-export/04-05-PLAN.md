---
phase: 04-csv-import-export
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/redirect-management/services/csv.service.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "parseCSV function compiles without TypeScript errors"
    - "CSV records are properly typed as Record<string, string>"
    - "Field access on parsed records has compile-time type checking"
  artifacts:
    - path: "src/plugins/redirect-management/services/csv.service.ts"
      provides: "Type-safe CSV parsing"
      contains: "as Array<Record<string, string>>"
  key_links:
    - from: "csv.service.ts parseCSV"
      to: "csv-parse library"
      via: "type assertion on parse() return value"
      pattern: "parse\\(.*\\)\\s*as\\s*Array<Record<string,\\s*string>>"
---

<objective>
Fix TypeScript TS18046 errors in parseCSV function by adding type assertion for csv-parse records.

Purpose: Restore type safety so TypeScript can verify field access on parsed CSV records, preventing potential runtime errors from malformed CSV data.

Output: Clean TypeScript compilation for csv.service.ts with no TS18046 errors.
</objective>

<execution_context>
@/Users/andrewhaas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewhaas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-csv-import-export/04-VERIFICATION.md

Source file with the gap:
@src/plugins/redirect-management/services/csv.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type assertion to csv-parse return value</name>
  <files>src/plugins/redirect-management/services/csv.service.ts</files>
  <action>
In the parseCSV function, add a type assertion to the parse() call return value.

Current code (lines 33-37):
```typescript
const records = parse(content, {
  columns: true,
  skip_empty_lines: true,
  trim: true
})
```

Change to:
```typescript
const records = parse(content, {
  columns: true,
  skip_empty_lines: true,
  trim: true
}) as Array<Record<string, string>>
```

This tells TypeScript that each record in the array is an object with string keys and string values, which matches how csv-parse works when `columns: true` is set (header values become keys, cell values become string values).

Do NOT change any other logic in the function - the parsing and field access code is already correct, it just needs the type annotation to satisfy the compiler.
  </action>
  <verify>
Run: `npx tsc --noEmit 2>&1 | grep "csv.service.ts" | wc -l`
Expected: 0 (no TypeScript errors in this file)

Also verify full type check:
Run: `npx tsc --noEmit 2>&1 | grep "TS18046" | wc -l`
Expected: Should show 0 TS18046 errors (the unknown type errors are resolved)
  </verify>
  <done>
csv.service.ts compiles without TypeScript errors. The 9 TS18046 errors on lines 45, 55-61 are resolved.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation succeeds for csv.service.ts with no TS18046 errors
2. CSV import functionality still works (existing import route uses parseCSV)
3. No runtime behavior changes - only compile-time type safety added
</verification>

<success_criteria>
- `npx tsc --noEmit` shows 0 errors containing "csv.service.ts" and "TS18046"
- parseCSV function correctly parses valid CSV content (no functional regression)
- Type assertion matches csv-parse library behavior when columns: true
</success_criteria>

<output>
After completion, create `.planning/phases/04-csv-import-export/04-05-SUMMARY.md`
</output>
