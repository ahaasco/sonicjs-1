---
phase: 04-csv-import-export
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/plugins/redirect-management/templates/redirect-list.template.ts
autonomous: false

must_haves:
  truths:
    - "Export CSV button is visible in redirect list header"
    - "Export button shows count of redirects that will be exported"
    - "Import form allows file upload with duplicate handling selection"
    - "Import form shows progress indicator during upload"
    - "Success/error messages display after import completes"
  artifacts:
    - path: "src/plugins/redirect-management/templates/redirect-list.template.ts"
      provides: "Export button and import form UI"
      contains: "Export CSV"
  key_links:
    - from: "redirect-list.template.ts Export button"
      to: "/admin/redirects/export"
      via: "href with current filter params"
      pattern: "href=\"/admin/redirects/export"
    - from: "redirect-list.template.ts Import form"
      to: "/admin/redirects/import"
      via: "hx-post multipart form"
      pattern: "hx-post=\"/admin/redirects/import\""
---

<objective>
Add CSV import/export UI elements to the redirect list page.

Purpose: Make import/export functionality accessible to admins through the existing redirect management interface. Export button shows redirect count and respects current filters. Import form handles file upload with duplicate handling options.

Output:
- Export CSV button in list header (with count)
- Import form with file input and duplicate handling radio buttons
- Progress indicator during import
- Success message display after operations
</objective>

<execution_context>
@/Users/andrewhaas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewhaas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-csv-import-export/04-CONTEXT.md
@.planning/phases/04-csv-import-export/04-02-SUMMARY.md
@.planning/phases/04-csv-import-export/04-03-SUMMARY.md
@src/plugins/redirect-management/templates/redirect-list.template.ts
@src/plugins/redirect-management/routes/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Export button and Import form to list template</name>
  <files>
    src/plugins/redirect-management/templates/redirect-list.template.ts
  </files>
  <action>
Modify redirect-list.template.ts to add CSV import/export UI:

1. Find the header section where "New Redirect" button is located. Add Export and Import controls next to it.

2. Add Export CSV button:
   - Button text: "Export CSV (N)" where N is the total count
   - Build href with current filter params: /admin/redirects/export?statusCode=X&matchType=Y&isActive=Z&search=S
   - Style: Secondary button (outline style, not primary color)
   - The count should match `pagination.total` from the template data

3. Add Import section with collapsible form:
```html
<!-- Import/Export section in header -->
<div class="flex items-center gap-3">
  <!-- Export button with count -->
  <a href="/admin/redirects/export${buildQueryString(filters)}"
     class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
    </svg>
    Export CSV (${pagination.total})
  </a>

  <!-- Import button (toggles form visibility) -->
  <button type="button"
          onclick="document.getElementById('import-form').classList.toggle('hidden')"
          class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
    </svg>
    Import CSV
  </button>

  <!-- Existing New Redirect button -->
  <a href="/admin/redirects/new" ...>New Redirect</a>
</div>

<!-- Import form (hidden by default) -->
<div id="import-form" class="hidden mt-4 p-4 bg-gray-50 dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700">
  <form hx-post="/admin/redirects/import"
        hx-encoding="multipart/form-data"
        hx-target="#import-result"
        hx-indicator="#import-progress"
        class="space-y-4">

    <div>
      <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
        Select CSV File
      </label>
      <input type="file"
             name="csv_file"
             accept=".csv"
             required
             class="block w-full text-sm text-gray-500 dark:text-gray-400
                    file:mr-4 file:py-2 file:px-4
                    file:rounded-lg file:border-0
                    file:text-sm file:font-medium
                    file:bg-blue-50 file:text-blue-700
                    dark:file:bg-blue-900/20 dark:file:text-blue-400
                    hover:file:bg-blue-100 dark:hover:file:bg-blue-900/30">
      <p class="mt-1 text-xs text-gray-500 dark:text-gray-400">
        Maximum 10MB, up to 10,000 rows
      </p>
    </div>

    <div>
      <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
        Duplicate Handling
      </label>
      <div class="space-y-2">
        <label class="flex items-center gap-2">
          <input type="radio" name="duplicate_handling" value="reject" checked
                 class="text-blue-600 focus:ring-blue-500">
          <span class="text-sm text-gray-700 dark:text-gray-300">
            Reject file if duplicates found
          </span>
        </label>
        <label class="flex items-center gap-2">
          <input type="radio" name="duplicate_handling" value="skip"
                 class="text-blue-600 focus:ring-blue-500">
          <span class="text-sm text-gray-700 dark:text-gray-300">
            Skip duplicate rows (import new only)
          </span>
        </label>
        <label class="flex items-center gap-2">
          <input type="radio" name="duplicate_handling" value="update"
                 class="text-blue-600 focus:ring-blue-500">
          <span class="text-sm text-gray-700 dark:text-gray-300">
            Update existing redirects with CSV values
          </span>
        </label>
      </div>
    </div>

    <div class="flex items-center gap-3">
      <button type="submit"
              class="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700">
        Upload and Import
      </button>
      <div id="import-progress" class="htmx-indicator">
        <svg class="animate-spin h-5 w-5 text-blue-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
      </div>
    </div>

    <div id="import-result"></div>
  </form>
</div>
```

4. Add helper function to build query string from filters:
```typescript
function buildQueryString(filters: { search?: string; statusCode?: string; matchType?: string; isActive?: string }): string {
  const params = new URLSearchParams()
  if (filters.search) params.set('search', filters.search)
  if (filters.statusCode) params.set('statusCode', filters.statusCode)
  if (filters.matchType) params.set('matchType', filters.matchType)
  if (filters.isActive) params.set('isActive', filters.isActive)
  const str = params.toString()
  return str ? `?${str}` : ''
}
```

5. Add success message display at top of list (check for ?success= query param):
   - The admin.ts route redirects with ?success=message after successful import
   - Parse the success param and display an alert if present

```html
${successMessage ? `
  <div class="rounded-lg border border-green-200 dark:border-green-800 bg-green-50 dark:bg-green-900/20 text-green-800 dark:text-green-400 p-4 mb-4">
    <p class="text-sm">${successMessage}</p>
  </div>
` : ''}
```

Update the renderRedirectListPage function signature to accept successMessage parameter, and modify admin.ts GET / route to parse ?success query param and pass it to the template.
  </action>
  <verify>
    - npx tsc --noEmit compiles without errors
    - Start dev server and visit /admin/redirects
    - Export button visible with correct count
    - Import button toggles form visibility
    - Form accepts file upload and duplicate handling selection
  </verify>
  <done>
    - Export CSV button shows in header with redirect count
    - Export link includes current filter parameters
    - Import form visible when Import CSV clicked
    - File input accepts .csv files only
    - Duplicate handling radio buttons present (reject/skip/update)
    - Progress indicator shows during upload
    - Import result area ready for HTMX responses
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire success message from import redirect</name>
  <files>
    src/plugins/redirect-management/routes/admin.ts
    src/plugins/redirect-management/templates/redirect-list.template.ts
  </files>
  <action>
1. Update admin.ts GET / route to parse success message:
```typescript
// In the GET / handler, after other query params:
const successMessage = c.req.query('success') || undefined

// Pass to template:
const html = renderRedirectListPage({
  redirects,
  pagination: { page, limit, total, totalPages },
  filters: { search, statusCode: statusCodeParam, matchType: matchTypeParam, isActive: isActiveParam },
  user: c.get('user'),
  successMessage  // Add this
})
```

2. Update renderRedirectListPage function in redirect-list.template.ts:
   - Add successMessage?: string to the options type
   - Display the success alert at top of content if successMessage exists

3. Handle the error CSV download case:
   - When import returns error CSV (400 status), the browser will download the file
   - Since it's a file download, the page won't update - user keeps their current view
   - The error CSV filename (import-errors.csv) tells them what happened

4. Consider edge case: if HTMX doesn't follow the redirect properly after success:
   - The import route returns 303 redirect to /admin/redirects?success=...
   - HTMX may swap the redirect response into #import-result instead of following it
   - To fix, add hx-target="body" or handle with HX-Redirect header

Update import route to use HX-Redirect header for HTMX compatibility:
```typescript
// Instead of:
return c.redirect(`/admin/redirects?success=${encodeURIComponent(message)}`, 303)

// Use:
return new Response(null, {
  status: 200,
  headers: {
    'HX-Redirect': `/admin/redirects?success=${encodeURIComponent(message)}`
  }
})
```

This tells HTMX to do a full page redirect to show the success message.
  </action>
  <verify>
    - Import CSV with valid file -> page shows success message
    - Import CSV with errors -> browser downloads import-errors.csv
    - Success message displays in green alert box
    - Success message disappears on next page load
  </verify>
  <done>
    - Success message displays after successful import
    - Error CSV downloads automatically on validation failure
    - Full import/export workflow is functional end-to-end
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete CSV import/export UI integrated into redirect list page:
    - Export CSV button with count in header
    - Import CSV button that reveals upload form
    - File upload with duplicate handling options
    - Progress indicator during import
    - Success/error message display
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to http://localhost:8788/admin/redirects
    3. Verify Export CSV button shows count (e.g., "Export CSV (5)")
    4. Click Export CSV - verify file downloads with correct name
    5. Click Import CSV - verify form appears
    6. Test import with a valid CSV file:
       - Create test.csv with: source_url,destination_url,match_type,status_code,active
       - Add row: /test-import,/destination,exact,301,true
       - Upload and verify success message
    7. Test import with invalid CSV (bad status code) - verify error CSV downloads
    8. Apply a filter (e.g., Status: 301) and export - verify filename includes filter
  </how-to-verify>
  <resume-signal>Type "approved" if UI works correctly, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Export button visible and functional with count
3. Import form appears on button click
4. Valid CSV imports successfully with message
5. Invalid CSV returns downloadable error report
6. Filters are preserved in export URL
</verification>

<success_criteria>
- Export CSV button shows redirect count matching pagination total
- Export respects current filters (filename reflects active filters)
- Import form has file input, duplicate handling options, and submit button
- Progress indicator appears during upload
- Success message displays after successful import
- Error CSV downloads when validation fails
- Full workflow tested end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-csv-import-export/04-04-SUMMARY.md`
</output>
