---
phase: 01-foundation-plugin-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/plugins/index.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Plugin is registered and appears in SonicJS plugin system"
    - "Plugin lifecycle hooks (install, activate, deactivate, uninstall) can execute"
  artifacts:
    - path: "src/index.ts"
      provides: "Plugin import and route mounting"
      contains: "import redirectManagementPlugin"
    - path: "src/plugins/index.ts"
      provides: "Plugin export for discovery"
      contains: "redirectManagementPlugin"
  key_links:
    - from: "src/index.ts"
      to: "src/plugins/redirect-management/index.ts"
      via: "import statement"
      pattern: "import.*redirectManagementPlugin.*from.*redirect-management"
    - from: "src/plugins/index.ts"
      to: "src/plugins/redirect-management/index.ts"
      via: "export statement"
      pattern: "export.*redirectManagementPlugin"
---

<objective>
Wire redirect-management plugin into SonicJS application by adding missing imports, exports, and route mounting.

Purpose: Phase 1 verification found plugin files exist but are not connected to the application. Without wiring, the plugin is dead code that cannot execute.
Output: Plugin registered, routes mounted, lifecycle hooks executable
</objective>

<execution_context>
@/Users/andrewhaas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewhaas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-plugin-structure/01-01-SUMMARY.md

# Reference files for wiring patterns
@src/plugins/contact-form/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export redirect plugin from plugins index</name>
  <files>src/plugins/index.ts</files>
  <action>
Add export statement for redirect-management plugin following the contact-form pattern:

1. Add export statement after the existing contactFormPlugin export:
   `export { default as redirectManagementPlugin } from './redirect-management/index'`

This enables plugin discovery by the framework and other components.
  </action>
  <verify>grep "redirectManagementPlugin" src/plugins/index.ts returns the export line</verify>
  <done>redirectManagementPlugin is exported from src/plugins/index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Import and mount redirect plugin in main app</name>
  <files>src/index.ts</files>
  <action>
Wire redirect-management plugin into the application following the contact-form pattern:

1. Add import statement after the contact-form import (line 17):
   `import redirectManagementPlugin from './plugins/redirect-management/index'`

2. Add 'redirect-management' to the enabled plugins array (line 35):
   Change: `enabled: ['email', 'contact-form']`
   To: `enabled: ['email', 'contact-form', 'redirect-management']`

3. Add route mounting after the contact-form routes block (after line 51):
   ```
   // Mount redirect management plugin routes
   if (redirectManagementPlugin.routes) {
     for (const route of redirectManagementPlugin.routes) {
       app.route(route.path, route.handler)
     }
   }
   ```

This connects the plugin to the application so lifecycle hooks execute and routes are available.
  </action>
  <verify>
Run: grep -E "redirectManagementPlugin|redirect-management" src/index.ts
Expected: Shows import statement, enabled array entry, and route mounting block
  </verify>
  <done>
- redirectManagementPlugin is imported from ./plugins/redirect-management/index
- 'redirect-management' appears in enabled plugins array
- Plugin routes are mounted via app.route()
  </done>
</task>

</tasks>

<verification>
After completion, verify plugin is fully wired:

1. Check export exists:
   `grep "redirectManagementPlugin" src/plugins/index.ts`
   Expected: export statement present

2. Check import and mounting:
   `grep -c "redirectManagementPlugin" src/index.ts`
   Expected: 3 occurrences (import, route check, route loop)

3. Check enabled plugins:
   `grep "enabled:" src/index.ts`
   Expected: includes 'redirect-management'

4. TypeScript compilation:
   `npx tsc --noEmit src/index.ts src/plugins/index.ts`
   Expected: No errors (plugin types resolve correctly)
</verification>

<success_criteria>
- redirectManagementPlugin exported from src/plugins/index.ts
- redirectManagementPlugin imported in src/index.ts
- 'redirect-management' in enabled plugins array
- Plugin routes mounted following contact-form pattern
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-plugin-structure/01-02-SUMMARY.md`
</output>
