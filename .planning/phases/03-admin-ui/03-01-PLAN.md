---
phase: 03-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/redirect-management/routes/admin.ts
  - src/plugins/redirect-management/templates/redirect-list.template.ts
  - src/plugins/redirect-management/index.ts
autonomous: true

must_haves:
  truths:
    - "Admin can click Redirects link in admin menu and see redirect management page"
    - "Admin can see table listing all existing redirects with source, destination, status code, match type, active status"
    - "Redirects table shows empty state message when no redirects exist"
    - "All table columns are sortable (source, destination, status code, match type, active)"
  artifacts:
    - path: "src/plugins/redirect-management/routes/admin.ts"
      provides: "Hono route handlers for admin UI pages"
      exports: ["createRedirectAdminRoutes"]
    - path: "src/plugins/redirect-management/templates/redirect-list.template.ts"
      provides: "List view template composition"
      exports: ["renderRedirectListPage"]
  key_links:
    - from: "src/plugins/redirect-management/routes/admin.ts"
      to: "src/plugins/redirect-management/services/redirect.ts"
      via: "RedirectService.list() and count() calls"
      pattern: "redirectService\\.(list|count)"
    - from: "src/plugins/redirect-management/routes/admin.ts"
      to: "src/plugins/redirect-management/templates/redirect-list.template.ts"
      via: "renderRedirectListPage() call"
      pattern: "renderRedirectListPage"
    - from: "src/plugins/redirect-management/index.ts"
      to: "src/plugins/redirect-management/routes/admin.ts"
      via: "Route registration via builder.addRoutes()"
      pattern: "createRedirectAdminRoutes"
---

<objective>
Create the admin redirect list page with route handlers and table template

Purpose: Enables admins to view all redirects in a sortable, paginated table - the foundational UI for redirect management
Output: Working /admin/redirects page showing redirect list with sortable columns
</objective>

<execution_context>
@/Users/andrewhaas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewhaas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-ui/03-CONTEXT.md
@.planning/phases/03-admin-ui/03-RESEARCH.md

# Phase 2 provides the service layer
@src/plugins/redirect-management/services/redirect.ts
@src/plugins/redirect-management/types.ts
@src/plugins/redirect-management/index.ts

# Core templates to use
@packages/core/src/templates/table.template.ts
@packages/core/src/templates/pagination.template.ts
@packages/core/src/templates/layouts/admin-layout-catalyst.template.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create redirect list template</name>
  <files>src/plugins/redirect-management/templates/redirect-list.template.ts</files>
  <action>
Create a redirect list page template that composes existing SonicJS templates:

1. Import core templates:
   - renderAdminLayoutCatalyst from @sonicjs-cms/core (or relative path to packages/core/src/templates/layouts/admin-layout-catalyst.template.ts)
   - renderTable from @sonicjs-cms/core templates
   - renderPagination from @sonicjs-cms/core templates
   - getConfirmationDialogScript from @sonicjs-cms/core templates

2. Define interfaces:
   - RedirectListPageData: redirects array, pagination info, filters, user

3. Implement renderRedirectListPage(data: RedirectListPageData): string
   - Page title: "Redirects" with "New Redirect" button linking to /admin/redirects/new
   - Table columns (all sortable):
     - Source URL (truncate to 50 chars with title tooltip)
     - Destination URL (truncate to 50 chars with title tooltip)
     - Status Code (render as badge: green for 301, blue for 302, etc.)
     - Match Type (render: 0="Exact", 1="Partial", 2="Regex")
     - Active (render as toggle indicator: green dot for active, gray for inactive)
     - Actions column: Edit button, Delete button
   - Use renderTable with selectable: true for bulk operations
   - Use renderPagination with page size selector
   - Empty state: "No redirects created yet. Click 'New Redirect' to get started."
   - Include getConfirmationDialogScript() in output for delete modals

4. Helper functions:
   - truncateUrl(url: string, maxLength: number): string - truncate with ellipsis
   - renderStatusBadge(code: number): string - colored badge for status codes
   - renderMatchTypeBadge(type: number): string - label for match type
   - renderActiveIndicator(active: boolean): string - green/gray dot

Follow existing SonicJS template patterns from admin-content-list.template.ts. Use Tailwind classes consistent with dark mode (dark: variants).
  </action>
  <verify>
TypeScript compiles: `cd /Users/andrewhaas/Projects/SonicJS/sonicjs/my-sonicjs-app && npx tsc --noEmit --skipLibCheck`
File exists with exports: renderRedirectListPage function exported
  </verify>
  <done>
List template renders redirect data into HTML table with sortable columns, truncated URLs, status badges, and pagination
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin route handlers</name>
  <files>src/plugins/redirect-management/routes/admin.ts</files>
  <action>
Create Hono route handlers for the redirect admin UI:

1. Import dependencies:
   - Hono from 'hono'
   - RedirectService from '../services/redirect'
   - renderRedirectListPage from '../templates/redirect-list.template'
   - Types: RedirectFilter, MatchType, StatusCode

2. Export function createRedirectAdminRoutes(): Hono
   - Create new Hono router

3. GET /admin/redirects (list page):
   - Extract query params: page (default 1), limit (default 20), search, status, matchType, isActive
   - Build RedirectFilter from params
   - Create RedirectService with c.env.DB
   - Call service.list(filter) and service.count(filter) in parallel
   - Calculate pagination: totalPages, startItem, endItem
   - Call renderRedirectListPage with data
   - Return c.html(result)

4. Query param parsing:
   - page: parseInt or default 1
   - limit: parseInt or default 20
   - status: parse to StatusCode if valid (301, 302, 307, 308, 410)
   - matchType: parse to MatchType if valid (0, 1, 2)
   - isActive: 'true' -> true, 'false' -> false, undefined -> undefined (show all)
   - search: string or undefined

5. Build baseUrl for pagination: '/admin/redirects' with preserved filter params

Follow pattern from database-tools-plugin admin routes. No auth middleware yet (will be added when routes are mounted).
  </action>
  <verify>
TypeScript compiles: `cd /Users/andrewhaas/Projects/SonicJS/sonicjs/my-sonicjs-app && npx tsc --noEmit --skipLibCheck`
Exports createRedirectAdminRoutes function
  </verify>
  <done>
Route handler for /admin/redirects fetches redirect data via service and renders list template
  </done>
</task>

<task type="auto">
  <name>Task 3: Register admin routes in plugin</name>
  <files>src/plugins/redirect-management/index.ts</files>
  <action>
Update the redirect plugin to mount admin routes:

1. Import createRedirectAdminRoutes from './routes/admin'

2. In createRedirectPlugin function, add routes via builder:
   - Call builder.addRoutes() with the admin routes
   - OR if builder.addRoutes doesn't exist, export the routes for manual mounting

3. Update menu item URL to point to /admin/redirects (change from /admin/plugins/redirect-management/settings)

4. If PluginBuilder doesn't have addRoutes method:
   - Export createRedirectAdminRoutes from the plugin index
   - Document that routes need manual mounting in app

Look at how database-tools-plugin or other plugins register admin routes. Follow the same pattern.
  </action>
  <verify>
TypeScript compiles: `cd /Users/andrewhaas/Projects/SonicJS/sonicjs/my-sonicjs-app && npx tsc --noEmit --skipLibCheck`
createRedirectAdminRoutes is exported from plugin
  </verify>
  <done>
Plugin exports admin routes for mounting, menu item points to /admin/redirects
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Route handler exists for GET /admin/redirects
3. Template renders redirect list with table structure
4. Plugin exports route creator function
</verification>

<success_criteria>
- GET /admin/redirects returns HTML page with redirect table
- Empty state shows when no redirects exist
- Table columns are sortable (client-side via renderTable script)
- Pagination controls render when multiple pages exist
- Plugin menu item points to correct URL
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-ui/03-01-SUMMARY.md`
</output>
