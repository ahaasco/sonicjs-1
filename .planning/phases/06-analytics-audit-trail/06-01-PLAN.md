---
phase: 06-analytics-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/034_add_updated_by_to_redirects.sql
  - src/plugins/redirect-management/types.ts
autonomous: true

must_haves:
  truths:
    - "updated_by column exists in redirects table"
    - "Redirect interface includes hitCount, createdByName, updatedByName fields"
    - "Migration can run without errors"
  artifacts:
    - path: "migrations/034_add_updated_by_to_redirects.sql"
      provides: "updated_by column and index"
      contains: "ALTER TABLE redirects ADD COLUMN updated_by"
    - path: "src/plugins/redirect-management/types.ts"
      provides: "Extended Redirect interface with analytics/audit fields"
      contains: "hitCount"
  key_links:
    - from: "migrations/034_add_updated_by_to_redirects.sql"
      to: "redirects table"
      via: "ALTER TABLE"
      pattern: "updated_by TEXT REFERENCES users"
---

<objective>
Add database column and TypeScript types to support analytics display and audit tracking.

Purpose: Foundation for displaying hit counts and audit information in admin UI
Output: Migration file and updated type definitions
</objective>

<execution_context>
@/Users/andrewhaas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewhaas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-audit-trail/06-RESEARCH.md
@src/plugins/redirect-management/types.ts
@migrations/032_redirect_plugin.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for updated_by column</name>
  <files>migrations/034_add_updated_by_to_redirects.sql</files>
  <action>
Create migration file with:

1. Add updated_by column to redirects table (TEXT, REFERENCES users(id), nullable for existing rows)
2. Create index on updated_by for JOIN performance
3. Backfill existing records: SET updated_by = created_by WHERE updated_by IS NULL

SQL pattern from RESEARCH.md:
```sql
-- Add updated_by column to track who last modified each redirect
ALTER TABLE redirects ADD COLUMN updated_by TEXT REFERENCES users(id);

-- Add index for JOIN performance
CREATE INDEX IF NOT EXISTS idx_redirects_updated_by ON redirects(updated_by);

-- Backfill existing records (set updated_by = created_by for existing redirects)
UPDATE redirects SET updated_by = created_by WHERE updated_by IS NULL;
```

Use next migration number: 034
  </action>
  <verify>
File exists at migrations/034_add_updated_by_to_redirects.sql with:
- ALTER TABLE for updated_by column
- CREATE INDEX statement
- UPDATE for backfill
  </verify>
  <done>Migration file created with updated_by column, index, and backfill statement</done>
</task>

<task type="auto">
  <name>Task 2: Extend types for analytics and audit data</name>
  <files>src/plugins/redirect-management/types.ts</files>
  <action>
Add optional fields to the Redirect interface for analytics and audit data that will be populated by LEFT JOINs:

1. Add to Redirect interface (as optional fields since they're populated via JOIN):
   - hitCount?: number (from redirect_analytics.hit_count)
   - lastHitAt?: number | null (from redirect_analytics.last_hit_at)
   - createdByName?: string (from users table via created_by JOIN)
   - updatedByName?: string (from users table via updated_by JOIN)
   - updatedBy?: string (the updated_by user ID)

2. These are optional because:
   - hitCount: New redirects have no analytics row yet (LEFT JOIN returns null)
   - lastHitAt: May be null if never hit
   - createdByName/updatedByName: Populated only when listing with JOINs
   - updatedBy: New column, may be null for old records

Pattern: Add after existing Redirect interface fields (after preserveQueryParams)
  </action>
  <verify>
Run: `grep -n "hitCount" src/plugins/redirect-management/types.ts`
Expect: Line with "hitCount?: number"
  </verify>
  <done>Redirect interface extended with hitCount, lastHitAt, createdByName, updatedByName, updatedBy optional fields</done>
</task>

</tasks>

<verification>
1. Migration file exists and is syntactically correct SQL
2. Types file compiles without TypeScript errors: `npx tsc --noEmit src/plugins/redirect-management/types.ts`
3. All required fields present in types
</verification>

<success_criteria>
- [ ] Migration 034 created with updated_by column, index, and backfill
- [ ] Redirect interface has hitCount, lastHitAt, createdByName, updatedByName, updatedBy fields
- [ ] No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-audit-trail/06-01-SUMMARY.md`
</output>
